server:
  port: 9091

spring:
  datasource:
    driver-class-nam: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/Java6MicroTekrarProductDB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

authservice:
  bunu-ben-yazdım:
    secret-key: ${JAVA6_SECRETKEY}
    issuer: ${JAVA6_ISSUER}
